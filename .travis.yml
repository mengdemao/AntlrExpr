language: cpp

dist: focal

branches:
  only:
  - master

addons:
  apt:
    packages:
      - cmake-data
      - cmake

compiler:
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make test

after_success:
    - echo "Build Success"